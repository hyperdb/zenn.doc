---
title: "［AtCoder］ABC-050｜C - Lining Up"
emoji: "⌨️"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ['python', 'AtCoder', 'abc']
published: true
---

## 設問ページ

- 🔗[ABC-050 C - Lining Up](https://atcoder.jp/contests/abc050/tasks/arc066_a)

## 提出結果

```python
def getInt():
    return int(input())


def getIntList():
    return list(map(int, input().split()))


def main():
    N = getInt()
    A = getIntList()
    A.sort()

    B = 10**9 + 7

    r = True
    # Nが奇数と偶数で場合分け
    if N % 2 == 1:
        # 先頭は0でなければならない
        if A[0] != 0:
            r = False
        # 2個ずつ確認 (同値で偶数でなければならない)
        for i in range(1, N, 2):
            if A[i] != A[i + 1]:
                r = False
                break
            if A[i] % 2 != 0:
                r = False
                break
    else:
        # 端数は出ないので0チェック不要
        # 2個ずつ確認 (同値で奇数でなければならない)
        for i in range(0, N, 2):
            if A[i] != A[i + 1]:
                r = False
                break
            if A[i] % 2 == 0:
                r = False
                break

    print((2 ** (N // 2)) % B if r else 0)


if __name__ == "__main__":
    main()
```
